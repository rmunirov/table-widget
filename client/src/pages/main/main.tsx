import React from 'react';
import TableWithFilter from '../../widgets/table-with-filter/table-with-filter';
import { Wrapper } from './main.styles';

const MainPage = () => {
    return (
        <Wrapper>
            <TableWithFilter />
        </Wrapper>
    );
};

export default MainPage;
